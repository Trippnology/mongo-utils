{
  "name": "@trippnology/mongo-utils",
  "description": "Friendly interface to mongodump and mongorestore commands.",
  "keywords": [
    "mongodb",
    "mongodump",
    "mongorestore",
    "heroku",
    "mongohq"
  ],
  "version": "1.0.1",
  "directories": {
    "lib": "./lib"
  },
  "main": "lib/utils.js",
  "dependencies": {
    "heroku": "7.62.0",
    "mongoson": "0.2.x"
  },
  "devDependencies": {
    "coffee-script": "1.6.x",
    "mocha": "10.0.0"
  },
  "engines": {
    "node": ">=0.8",
    "npm": ">=1.1"
  },
  "author": "Meryn Stol <merynstol@gmail.com>",
  "homepage": "https://github.com/meryn/mongo-utils",
  "repository": {
    "type": "git",
    "url": "git://github.com/meryn/mongo-utils.git"
  },
  "scripts": {
    "prepublish": "npm test",
    "preversion": "coffee --compile -m --output lib/ src/",
    "pretest": "coffee --compile -m --output lib/ src/",
    "test": "mocha --recursive --exit"
  }
}
